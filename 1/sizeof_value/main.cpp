#include <iostream.h>
void main()
{
	int  n ; char ch;float f ; double d;
	int  nm = sizeof(n); int nch = sizeof(ch); int nf = sizeof(f); int nd = sizeof(d);
	cout<<"sizeof(nm)="<<nm<<endl;
	cout<<"sizeof(nm)="<<nch<<endl;
	cout<<"sizeof(nm)="<<nf<<endl;
	cout<<"sizeof(nm)="<<nd<<endl;
}